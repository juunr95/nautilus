/*
 * Default unauthorized text.
 */
const UNAUTHORIZED = 'Unauthorized';

/*
 * Expected keys on options object.
 */
const EXPECTED_KEYS = ['username', 'password'];

module.exports = {
  UNAUTHORIZED,
  EXPECTED_KEYS,
}